# 7 Table Commands

To Use the Table

```
use students;
```

See Everything inside a Table

```
select * from students;
```

Create Table

```
create table students (rollno int primary key, name varchar(50));
```

Insert into the Table

```
insert into students (rollno, name) values (1, "Ram") , (2, "Shyam");
```
